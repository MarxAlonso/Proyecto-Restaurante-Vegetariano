-- Script de inicialización ejecutado por PostgreSQL al primer inicio
-- Se ejecuta solo si la base de datos está vacía

CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
CREATE EXTENSION IF NOT EXISTS "pgcrypto";
